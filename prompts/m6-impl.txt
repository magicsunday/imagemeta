Repo: MagicSunday/ImageMeta
Task: Implementiere „M6 – MakerNotes aktivieren (Registry + erste Decoder)“ und erstelle einen Pull Request.

Kontext / Leitplanken
- PHP 8.4, strict_types=1, PSR-12.
- MakerNotes-Tag: 0x927C; Offsets häufig **relativ zum MakerNote-Block**.
- Bereits vorhanden (ggf. als Stubs): `MakerNotes\Registry`, `AppleDecoder`.

Ziel (Issue M6)
1) `TiffExifReader` hooken: Wenn Tag 0x927C vorhanden, `Make/Model` (aus IFD0/ExifIFD) bestimmen und über `MakerNotes\Registry` passenden Decoder aufrufen.
2) Erste Decoder (best effort):
   - `AppleDecoder` vervollständigen (z. B. HDR-Hints/Software/Build), sonst Roh-Digest.
   - `CanonDecoder`, `NikonDecoder`, `SonyDecoder` – je mind. 3 populäre Felder (synthetisch).
3) Ergebnisstruktur: `makerNotes` als `array<string,mixed>` im `ExifDocument` **oder** in `Metadata` (ohne API-Bruch; optional-nullbar).
4) Fallback: unbekannter Hersteller → Roh-Digest (Länge + SHA1), keine Exceptions.

Änderungs-Scope
- ÄNDERN: `src/Parse/Tiff/TiffExifReader.php` (Hook + Übergabe Basis-Offset an Decoder)
- ÄNDERN: `src/MakerNotes/Registry.php`
- NEU: `src/MakerNotes/{CanonDecoder.php,NikonDecoder.php,SonyDecoder.php}`
- ÄNDERN (modellseitig, rückwärtskompatibel): `src/Model/Exif/ExifDocument.php` **oder** `src/Model/Metadata.php` um `makerNotes`-Feld (nullable)
- TESTS NEU: `tests/MakerNotes/{AppleDecoderTest.php,CanonDecoderTest.php,NikonDecoderTest.php,SonyDecoderTest.php}`

Tests (synthetische Blobs)
- `testAppleDecoderReturnsHintsOrDigest()` – iPhone-ähnlicher Block → einige Felder oder Digest.
- `testCanonNikonSonyReturnAtLeastThreeFields()` – je vendor synthetische Strukturen → min. 3 Felder.
- `testUnknownVendorFallsBackToDigest()` – unbekannter Make → Digest, keine Exception.
- `testRelativeOffsetsHandled()` – Offsets relativ zum Block korrekt interpretiert.

Qualität & Guards
- Keine Exceptions bei unbekannten/unvollständigen Notes; defensiv parsen.
- Dokumentiere im PR, welche Felder best-effort sind.

Arbeitsweise (Codex bitte automatisieren)
1) Branch `feat/m6-makernotes`.
2) Implementierung + Tests.
3) PHPUnit/PHPStan fixen.
4) PR erstellen:
   - Title: `feat(makernotes): enable registry and add Apple/Canon/Nikon/Sony decoders`
   - Body: Überblick; „Closes #5“.
   - Labels: `type:feature`, `area:makernotes`
   - Milestone: `M6 – MakerNotes aktivieren`
